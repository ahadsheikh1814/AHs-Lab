---
title: Spotlight Card
description: A radial gradient that follows the mouse cursor as it moves across the card.
icon: Spotlight
---

import SpotlightCard from "@/resistry/SpotlightCard";

## Introduction

A radial gradient that follows the mouse cursor as it moves across the card.

## Demo
<Preview link="SpotlightCard">
<SpotlightCard/>
</Preview>


```npm
npx shadcn@latest add https://ahs-lab.vercel.app/r/SpotlightCard.json
```
# Or

## Code
```tsx tab="tsx"
"use client";
import { motion, useMotionTemplate, useMotionValue } from "motion/react";
import { MouseEvent } from "react";

export default function SpotlightCard() {
  const mouseX = useMotionValue(0);
  const mouseY = useMotionValue(0);

  function handleMouseMove({
    currentTarget,
    clientX,
    clientY,
  }: MouseEvent) {
    const { left, top } = currentTarget.getBoundingClientRect();

    mouseX.set(clientX - left);
    mouseY.set(clientY - top);
  }

  return (
    <div
      className="group relative max-w-md rounded-xl border border-white/10 bg-neutral-800 px-8 py-16 shadow-2xl"
      onMouseMove={handleMouseMove}
    >
      <motion.div
        className="pointer-events-none absolute -inset-px rounded-xl opacity-0 transition duration-300 group-hover:opacity-100"
        style={{
          background: useMotionTemplate`
            radial-gradient(
              650px circle at ${mouseX}px ${mouseY}px,
              rgba(14, 165, 233, 0.15) ,
              transparent 50%
            )
          `,
        }}
      />
      <div>
        <h3 className="text-base font-semibold tracking-tight leading-7 text-sky-300 dark:text-sky-700">
          AHs Lab
        </h3>
        <div className="mt-2 flex items-center gap-x-2">
          <span className="text-5xl font-bold tracking-tight text-white">
            Next Js
          </span>
        </div>
        <p className="mt-6 text-xl font-bold leading-7 text-gray-300">
          The React Framework for the Web <br />
          <span className="text-sm font-medium text-gray-400">Used by some of the {"world's"} largest companies, Next.js enables you to create high-quality web applications with the power of React components.</span>
        </p>
      </div>
    </div>
  );
}
```
```jsx tab="jsx"
"use client";

import { motion, useMotionTemplate, useMotionValue } from "motion/react";
import React from "react";

export default function SpotlightCard() {
  const mouseX = useMotionValue(0);
  const mouseY = useMotionValue(0);

  function handleMouseMove(e) {
    const rect = e.currentTarget.getBoundingClientRect();
    mouseX.set(e.clientX - rect.left);
    mouseY.set(e.clientY - rect.top);
  }

  function handleMouseLeave() {
    // move the spotlight off-card when the pointer leaves
    mouseX.set(-9999);
    mouseY.set(-9999);
  }

  return (
    <div
      role="article"
      aria-label="Spotlight card for AHs Lab — Next.js"
      className="group relative max-w-md rounded-xl border border-white/10 bg-neutral-800 px-8 py-16 shadow-2xl"
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
    >
      <motion.div
        className="pointer-events-none absolute -inset-px rounded-xl opacity-0 transition duration-300 group-hover:opacity-100"
        style={{
          // useMotionTemplate produces a reactive CSS string that updates with the motion values
          background: useMotionTemplate`
            radial-gradient(
              650px circle at ${mouseX}px ${mouseY}px,
              rgba(14,165,233,0.15),
              transparent 50%
            )
          `,
        }}
      />

      <div>
        <h3 className="text-base font-semibold tracking-tight leading-7 text-sky-300">
          AHs Lab
        </h3>

        <div className="mt-2 flex items-center gap-x-2">
          <span className="text-5xl font-bold tracking-tight text-white">
            Next Js
          </span>
        </div>

        <p className="mt-6 text-xl font-bold leading-7 text-gray-300">
          The React Framework for the Web
          <br />
          <span className="text-sm font-medium text-gray-400">
            Used by some of the world's largest companies, Next.js enables you to create
            high-quality web applications with the power of React components.
          </span>
        </p>
      </div>
    </div>
  );
}
```
